[project]
name = "mimo-vision-mcp"
version = "1.0.0"
description = "MCP bundle that exposes Xiaomi MiMo Vision image analysis as a stdio MCP tool."
requires-python = ">=3.10"
dependencies = [
  "openai>=1.0",
  "mcp>=1.0",
  "httpx>=0.27",
]

[project.license]
text = "MIT"

[tool.uv]
package = false
